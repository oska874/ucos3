AUX_SOURCE_DIRECTORY(. LIBHW_SRC)
AUX_SOURCE_DIRECTORY(./Posix/GNU LIBHW_SRC_1)
list(APPEND LIBHW_SRC ${LIBHW_SRC_1})

INCLUDE_DIRECTORIES(../uC-LIB)
INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(./Posix/GNU)

add_library(libhw ${LIBHW_SRC})
