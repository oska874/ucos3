
AUX_SOURCE_DIRECTORY(. LIBUC_SRC)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(../uC-CPU/Posix/GNU)
INCLUDE_DIRECTORIES(../uC-CPU)
INCLUDE_DIRECTORIES(../../Examples/POSIX/GNU/OS3)

add_library(libuc ${LIBUC_SRC})
